// This module's job is to verify all configs are parsable and all the entries are as per the types.
// import mainConf from "./conf/config.json5";

export async function areConfigsOkay(): Promise<boolean>
{


    return true;
}